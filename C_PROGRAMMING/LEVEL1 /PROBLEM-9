//get a 3 digit number from the user and print the hundered's digit.
#include <stdio.h>
int main()
{
    int a,b;
    printf("enter the three digit number:");
    scanf("%d",&a);
    b=a/100;
    printf("the hundered's digit is:%d",b);
    return 0;
}
